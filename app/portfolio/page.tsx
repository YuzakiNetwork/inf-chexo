'use client';
import { useState } from 'react';
import { Shell } from '@/components/shell';
import { portfolioItems } from '@/lib/data';
export default function Portfolio(){const [items,setItems]=useState(portfolioItems);const [open,setOpen]=useState(false);const [title,setTitle]=useState('');const [type,setType]=useState('Website');return <Shell><div className="container"><section className="page-head"><div className="eyebrow">Student portfolio</div><h1>Tempat karya dikumpulkan.</h1><p>Portfolio menjadi ruang untuk menampilkan website, program, UI/UX, poster digital, dan project Informatika siswa.</p></section><section className="section"><div className="section-head"><div><div className="eyebrow">My works</div><h2 className="section-title">Karya pilihan</h2></div><button className="button button-dark" onClick={()=>setOpen(!open)}>{open?'Tutup':'Tambah karya'}</button></div>{open&&<div className="card" style={{marginBottom:14}}><div className="form"><label>Judul karya</label><input className="input" value={title} onChange={e=>setTitle(e.target.value)} placeholder="Contoh: Website Perpustakaan"/><label>Jenis</label><select className="input" value={type} onChange={e=>setType(e.target.value)}><option>Website</option><option>Program JavaScript</option><option>UI/UX</option><option>Poster Digital</option></select><button className="button button-yellow" onClick={()=>{if(!title.trim())return;setItems(v=>[{title,type,desc:'Karya baru yang ditambahkan melalui portfolio.',tech:'CHEXO Demo'},...v]);setTitle('');setOpen(false)}}>Simpan karya</button></div></div>}<div className="grid grid-2">{items.map((x,i)=><article className="card" key={i}><span className="tag">{x.type}</span><h3>{x.title}</h3><p>{x.desc}</p><span className="status">{x.tech}</span></article>)}</div></section></div></Shell>}
