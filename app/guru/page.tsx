import Link from 'next/link';
import { Shell } from '@/components/shell';
import { RoleGuard } from '@/components/role-guard';
import { tasks } from '@/lib/data';
export default function Guru(){return <RoleGuard role="teacher"><Shell><div className="container"><section className="dashboard"><div className="dash-top"><div><div className="eyebrow">Portal guru</div><h1>Kelola pembelajaran.</h1><p className="muted">Buat tugas, pantau submission, dan siapkan materi dari satu dashboard.</p></div><Link className="button button-dark" href="/tugas">Lihat tugas →</Link></div><div className="mini-stats"><div className="mini-stat"><span className="tag">Tugas aktif</span><strong>3</strong><span className="muted">dipublikasikan</span></div><div className="mini-stat"><span className="tag">Menunggu</span><strong>12</strong><span className="muted">submission contoh</span></div><div className="mini-stat"><span className="tag">Materi</span><strong>8</strong><span className="muted">materi inti</span></div></div><div style={{marginTop:22}} className="card"><div className="tag">Tugas aktif</div><div className="table-like">{tasks.map(t=><div className="row" key={t.id}><strong>{t.title}</strong><span>{t.subject}</span><span>{t.className}</span><span>{new Date(t.deadline).toLocaleDateString('id-ID')}</span></div>)}</div></div></section></div></Shell></RoleGuard>}
